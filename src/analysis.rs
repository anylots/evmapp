use ethereum_types::{Address, U256};
use evmapp::evm;
use evmapp::storage::ramdb;
use evmapp::types::{Env, RunResult};

fn main() {
    let data = String::from("sdgsdg");
    let data_re = &a;
    let data_into: String = data_re.into();

    let database = ramdb::RamDB::new();

    println!("{}", "run finish");
}
