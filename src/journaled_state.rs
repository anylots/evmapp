use std::iter::Map;
use crate::state::State;

pub struct JournaledState {
    // pub state: State,
}

impl JournaledState {
    pub fn new() -> JournaledState {
        Self {
            // state: Map::new(),
        }
    }

    pub fn load_account() {}
}
